# 오픈소스마케팅 회사 소개 덱

모회사 대표와 임원에게 회사의 가치를 설명하는 자리에서 쓰는 26장짜리 덱입니다. 오픈소스마케팅을
처음 들여다보는 사람이 보는 자리라, 실적 나열이 아니라 무슨 문제를 푸는 회사인지부터 세웁니다.

무엇을 하는 회사인지를 먼저 놓고, 일하는 방식과 운영 규모를 보인 뒤, 쌓인 것을 뒤쪽에 증거로
붙이고 앞으로로 마칩니다.

## 근거

| 부 | 근거 |
|---|---|
| 1부, 2부 | 2026년 2월 회사소개서와 osoma.kr. 고객사가 들고 오는 문제 넷과 분석 환경 설명은 53건에서 반복된 것을 정리했습니다 |
| 3부 | 2026년 관광기업 데이터와 AI 활용 지원사업 슬랙 워크스페이스. 채널 구조, 봇 리마인드, 회차 등록 형식, 진행률 기준표, 월간 보고 일정에서 뽑았습니다 |
| 4부 | 같은 워크스페이스의 컨설팅 기록. 실제 설치물과 구독 설계 방식입니다 |
| 5부 | 고객사와 연혁은 소개서 연혁의 2022년부터 2025년까지의 목록입니다. 앞으로 두 갈래는 소개서에 없는 내용이라 별도로 정한 방향입니다 |

산업군 분류는 연혁 53건을 성격으로 묶은 것입니다. 유통과 커머스 12건, IT와 금융 13건, 교육과 공공
10건, 제조와 서비스 18건이고 넷을 합하면 53건이 됩니다.

## 구성

| 부 | 장 | 내용 |
|---|---|---|
| 1부 무엇을 하는 회사인가 | 1~7 | 표지, 차례, 회사 개요, 고객사가 들고 오는 문제 넷, 분석 환경의 뜻, 일의 네 갈래, 맡긴 뒤 달라지는 것 |
| 2부 일하는 방식 | 8~11 | 구축 여섯 단계, 남기는 결과물 여섯, 그로스 마케팅, 인계 |
| 3부 스물네 곳을 동시에 | 12~18 | 사업 개요, 자리 정하기, 일정 관리, 주간 운영, 진행률 기준표, 보고 세 층, 변경 관리 |
| 4부 AI를 얹은 자리 | 19~22 | 달라진 일, AI를 심는 다섯 단계, 현장 설치물, 구독 설계 |
| 5부 쌓인 것과 앞으로 | 23~26 | 고객사 네 갈래, 연혁, 앞으로 두 갈래, 마무리 |

## 배치의 뜻

앞의 두 부가 회사의 값을 설명하고, 뒤의 두 부가 그것을 뒷받침합니다. 3부의 지원사업은 스물네 곳을
동시에 맡아도 방식이 무너지지 않는다는 근거로 놓았고, 5부의 고객사와 연혁은 자랑이 아니라 앞에서
말한 방식이 통한 자리로 놓았습니다. 순서를 바꾸면 다시 실적 소개가 되므로 장을 옮길 때 이 점을 봅니다.

## 만들기

```bash
python3 build_deck.py            # 본문_소개.md 를 읽어 덱을 만듭니다
python3 build_deck.py --approve  # 대기 문장을 검수하고 고친 뒤 승인합니다
python3 build_deck.py --export   # 코드 쪽 글로 본문_소개.md 를 다시 뽑습니다. 파일의 수정이 지워집니다
python3 build_deck.py --print    # 인쇄 전용 판
python3 export_pdf.py            # 한 장 한 쪽으로 PDF를 찍습니다. 구글 크롬과 pypdf가 필요합니다
```

화면에 보이는 글은 `본문_소개.md`에 장별로 한 줄에 한 덩이씩 들어 있습니다. 글만 고칠 때는 이 파일을
고치고 다시 빌드합니다. 줄을 더하거나 지우면 구조가 어긋나 빌드가 멈추고, 코드 기준 새 추출본을
`본문_소개.새로추출.md`에 써 줍니다. 장을 더하거나 빼는 일은 `build_deck.py`에서 합니다.

빌드가 금지 기호와 번역투, 헤더 종결어미를 먼저 검사합니다. 화면 문장이 바뀌면 `교정대기_소개.txt`가
생기고 빌드가 멈추므로, 그 문장을 검수하고 고친 뒤 `--approve`로 승인합니다.

덱 오른쪽 위 도구 모음에는 발표자 보기, 번호 이동, PDF 내려받기, 전체보기, 편집이 있습니다. 화면에서
고친 글은 브라우저에만 남으므로, 편집 바의 내보내기로 받은 `편집본_소개.json`을
`python3 apply_edits.py`로 되박아야 다음 빌드에 남습니다.

## 색

로고 파랑 `#3395DA`를 기준으로 삼고, 제목의 강조 낱말과 카드 테두리에 파랑에서 청록으로 가는
그라데이션을 씁니다.

```
#1B6FA8  →  #3395DA  →  #35B4C7  →  #6FD3C7
```

색을 바꾸려면 `deck_theme.py` 위쪽 `:root`의 `--c1`부터 `--c4`와 오로라 네 개의 rgba를 고칩니다.
SVG 안에서 쓰는 같은 그라데이션은 `GRAD_STOPS`가 한자리에서 만들어 냅니다.

## 파일 구성

```
build_deck.py       장 구성과 문장 30장, 조립. 화면에 보이는 글이 여기 있습니다
deck_theme.py       색과 공통 CSS, 공통 가구, 금지 기호 검사기, 교정 게이트
deck_parts.py       장을 짜는 조각들. 카드, 계단, 마주 놓기, 목록판, 아이콘, 일러스트 칸
sync_text.py        본문_소개.md 와 덱을 맞추는 모듈
toolbar.py          오른쪽 위 도구 모음
apply_edits.py      화면에서 고친 글을 본문 파일로 되박습니다
export_pdf.py       덱을 한 장 한 쪽으로 PDF로 찍습니다
prep_illus.py       삽화 배경을 순백으로 맞춥니다
assets/             로고와 이미지. logos/ 는 AI 서비스 파비콘입니다

시연물/              GA4 자료로 돌아가는 시연물 네 편의 빌더와 템플릿
  extract.py         GA4 조회 9종. 결과는 data/*.json
  campaigns.py       캠페인 이름 정리 규칙
  build_race.py      캠페인 모금 레이스
  build_graph.py     웹 구조 지도
  build_analog.py    표와 대시보드와 질의 화면
  build_orchestra.py 원격 지휘 연출 화면
  *_template.html    손볼 곳은 이 템플릿입니다
  shot.sh            헤드리스 크롬 갈무리
```

## 시연물

이 덱은 시연물을 넣지 않습니다. `시연물/` 은 앞선 프로젝트에서 가져온 틀이고, `data/`의 GA4 원자료는
가져오지 않아 지금은 돌지 않습니다. 새 자료로 쓰려면 다음을 손봅니다.

- `build_graph.py`의 `SITE_HOST`, `PAY_HOST`와 `build_analog.py`의 `HOSTS`를 고객사 도메인으로 바꿉니다
- `campaigns.py`의 `DROP_EXACT`와 `ALWAYS_ON`을 실제 값을 보고 채웁니다
- 화면 문구가 비영리 후원 흐름에 맞춰져 있습니다. `analog_template.html`의 분석 서술은 새 자료에 맞게 다시 씁니다
